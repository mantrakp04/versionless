# @versionless/query-catalog

The curated analytics query catalog used by `@versionless/cli`.

Most users should install the CLI rather than depend on this package directly:

```sh
bun add --dev @versionless/cli
```
